<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.himart.restservicecors.dao.LogDao">
	<insert id="insertLog" parameterType="LogDto" >
			insert into sv902m
			(Q_CODE,HRUSR_ID,REQ_DT,REQ_PUR)
			values
			(#{qCode},#{hrusrId},#{reqDt},#{reqPur})
	</insert>
</mapper>